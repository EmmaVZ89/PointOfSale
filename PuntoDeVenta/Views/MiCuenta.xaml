<Window x:Class="PuntoDeVenta.Views.MiCuenta"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PuntoDeVenta.Views"
        mc:Ignorable="d"
        Title="MiCuenta" WindowStyle="None"
        WindowState="Maximized" AllowsTransparency="True" Background="Transparent">

    <!-- OVERLAY QUE CUBRE TODA LA PANTALLA -->
    <Grid Background="#7F000000" MouseDown="Overlay_MouseDown">
        
        <!-- CARD CENTRADA -->
        <Border Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" Background="White" CornerRadius="10" MouseDown="Card_MouseDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>
            
            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Titulo -->
                    <RowDefinition Height="Auto"/> <!-- Foto -->
                    <RowDefinition Height="Auto"/> <!-- Datos -->
                    <RowDefinition Height="Auto"/> <!-- Boton -->
                </Grid.RowDefinitions>

                <!-- Titulo -->
                <TextBlock Text="Mi Perfil" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Foto -->
                <Border Grid.Row="1" Width="100" Height="100" CornerRadius="50" BorderBrush="#BDC3C7" BorderThickness="1" HorizontalAlignment="Center">
                    <Grid>
                        <Ellipse>
                            <Ellipse.Fill>
                                <ImageBrush x:Name="imagen" ImageSource="../src/img/user.png" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Grid>
                </Border>

                <!-- Datos -->
                <StackPanel Grid.Row="2" VerticalAlignment="Center" Margin="0,20">
                    <TextBlock x:Name="lblNombre" Text="Nombre Apellido" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="#34495E"/>
                    <TextBlock x:Name="lblRol" Text="Administrador" FontSize="14" HorizontalAlignment="Center" Foreground="#7F8C8D" Margin="0,5,0,15"/>
                    
                    <Separator Width="200" Margin="0,0,0,15"/>
                    
                    <Grid Margin="20,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Usuario:" FontWeight="Bold" Foreground="#7F8C8D"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblUsuario" Text="admin" HorizontalAlignment="Right" Foreground="#2C3E50"/>

                        <TextBlock Grid.Row="1" Text="Correo:" FontWeight="Bold" Foreground="#7F8C8D"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblCorreo" Text="admin@mail.com" HorizontalAlignment="Right" Foreground="#2C3E50"/>
                    </Grid>
                </StackPanel>

                <!-- Boton Cerrar -->
                <Button Grid.Row="3" Content="Cerrar" Height="40" Click="BtnCerrar_Click" Cursor="Hand" Margin="0,10,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#E74C3C"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
        
        <!-- LOADER LOCAL -->
        <Grid x:Name="LoadingOverlay" Background="#7F000000" Visibility="Collapsed" Panel.ZIndex="99">
            <Border Background="White" CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30,20">
                <StackPanel>
                    <ProgressBar IsIndeterminate="True" Height="10" Width="200" Margin="0,0,0,15" Foreground="{DynamicResource AccentColor}"/>
                    <TextBlock Text="Cargando..." FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
