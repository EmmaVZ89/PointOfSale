<Window x:Class="PuntoDeVenta.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PuntoDeVenta.Views"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style x:Key="InputBox" TargetType="Control">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        
        <!-- CONTENEDOR RAIZ QUE PERMITE SUPERPOSICION -->
        <Grid>
            <!-- GRID DEL FORMULARIO -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/> <!-- Imagen / Banner -->
                    <ColumnDefinition Width="*"/>   <!-- Formulario -->
                </Grid.ColumnDefinitions>

            <!-- Izquierda: Banner Visual -->
            <Border Grid.Column="0" CornerRadius="15,0,0,15" Background="{DynamicResource PrimaryColor}">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Icono App: Tienda (Store) -->
                        <Viewbox Width="100" Height="100" Margin="0,0,0,20">
                            <Path Data="M12,18H6V14H12M21,14V12L20,7H4L3,12V14H4V20H14V14H18V20H20V14M20,4H4V6H20V4Z" 
                                  Fill="White"/>
                        </Viewbox>
                        <TextBlock Text="Gestión de Ventas" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Bienvenido al Sistema" Foreground="White" Opacity="0.8" FontSize="16" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Derecha: Login Form -->
            <Grid Grid.Column="1" Margin="40">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Iniciar Sesión" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,30" HorizontalAlignment="Center"/>

                    <!-- Usuario -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Usuario" Foreground="#7F8C8D" FontSize="12" Margin="0,0,0,5"/>
                        <Grid>
                            <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z" 
                                  Fill="#BDC3C7" Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Stretch="Uniform"/>
                            <TextBox x:Name="tbUsuario" Style="{StaticResource InputBox}" Padding="25,0,0,0"
                                     KeyDown="TbUsuario_KeyDown"/>
                        </Grid>
                    </StackPanel>

                    <!-- Contraseña -->
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="Contraseña" Foreground="#7F8C8D" FontSize="12" Margin="0,0,0,5"/>
                        <Grid>
                            <Path Data="M12,17A2,2 0 0,0 14,19A2,2 0 0,0 16,17A2,2 0 0,0 14,15A2,2 0 0,0 12,17M18,8A2,2 0 0,1 20,10V20A2,2 0 0,1 18,22H6A2,2 0 0,1 4,20V10C4,8.89 4.9,8 6,8H7V6A5,5 0 0,1 12,1A5,5 0 0,1 17,6V8H18M12,3A3,3 0 0,0 9,6V8H15V6A3,3 0 0,0 12,3Z" 
                                  Fill="#BDC3C7" Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Stretch="Uniform"/>
                            <PasswordBox x:Name="pbContra" Style="{StaticResource InputBox}" Padding="25,0,0,0"
                                         KeyDown="PbContra_KeyDown"/>
                        </Grid>
                    </StackPanel>

                    <!-- Botones -->
                    <Button Content="ACCEDER" Height="45" Click="Acceder" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="25">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Salir" Margin="0,15,0,0" Click="Cerrar" Cursor="Hand" Foreground="#7F8C8D" Background="Transparent" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- LOADER LOCAL -->
        <Grid x:Name="LoadingOverlay" Background="#7F000000" Visibility="Collapsed" Panel.ZIndex="99">
            <Border Background="White" CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30,20">
                <StackPanel>
                    <ProgressBar IsIndeterminate="True" Height="10" Width="200" Margin="0,0,0,15" Foreground="{DynamicResource AccentColor}"/>
                    <TextBlock Text="Iniciando sesión..." FontSize="16" Foreground="{DynamicResource TextColor}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
        </Grid>
    </Border>
</Window>
